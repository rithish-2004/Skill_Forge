server:
  port: 8001

spring:
  application:
    name: identity-service
  datasource:
    url: jdbc:mysql://localhost:3306/skillforge_identity?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
<<<<<<< HEAD
    # --- ADDED THIS SECTION ---
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
    # --------------------------
=======
    prefer-ip-address: true
    hostname: localhost
>>>>>>> 7eab406 (added the api gateway logic)

skillforge:
  jwt:
    secret: skillforge-enterprise-jwt-secret-key-minimum-256-bits-required-for-hs256-algorithm-safety
    expiration-ms: 86400000